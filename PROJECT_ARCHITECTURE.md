# Chi Code 项目架构文档

> 指挥官必读：在派发任务前先了解项目全貌

## 项目概述

**Chi Code** 是基于 Zed 编辑器改造的智能自主编程 IDE，目标是实现 AI 驱动的自主编程能力。

## 项目状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 1: 品牌重塑 | ✅ 完成 | Cargo.toml、窗口类名、安装程序 |
| Phase 2: 自主编程核心 | ✅ 编译通过 | autonomous_agent crate |
| Phase 3: 多 AI 支持 | 🔄 进行中 | multi_model_dispatch 模块 |
| Phase 4: UI 集成 | 🔄 进行中 | cc_switch 面板 |

---

## 核心模块

### 1. autonomous_agent (自主编程核心)
**路径**: `crates/autonomous_agent/`
**状态**: ✅ 编译通过

| 文件 | 用途 |
|------|------|
| `lib.rs` | 主模块，AutonomousAgent 入口 |
| `task_planner.rs` | 任务分解器 |
| `executor.rs` | 执行引擎 |
| `progress.rs` | 进度追踪 |
| `multi_ai.rs` | 多 AI 协调 |

### 2. cc_switch (控制中心)
**路径**: `crates/cc_switch/`
**状态**: ✅ 测试通过 (32/32)

管理 AI 提供商、MCP 服务器、技能。

| 文件 | 用途 |
|------|------|
| `cc_switch.rs` | 主入口 |
| `cc_switch_panel.rs` | 面板 UI |
| `config_sync.rs` | 配置同步 |
| `models.rs` | 数据模型 |
| `views/*.rs` | 各种视图组件 |

### 3. multi_model_dispatch (多模型调度)
**路径**: `crates/multi_model_dispatch/`
**状态**: ✅ 编译通过

Architect → Coder → Reviewer 链式调度。

| 文件 | 用途 |
|------|------|
| `dispatcher.rs` | 核心调度器 |
| `agent/agent.rs` | Agent 角色定义 |
| `settings.rs` | 配置 |

### 4. i18n (国际化)
**路径**: `crates/i18n/`
**状态**: ✅ 测试通过 (12/12)

| 文件 | 用途 |
|------|------|
| `locales/en-US/main.ftl` | 英文翻译 |
| `locales/zh-CN/main.ftl` | 中文翻译 |

---

## 待开发模块

### 优先级 1: UI 集成
- [ ] 自主模式 UI 面板 (agent_ui)
- [ ] 任务输入界面
- [ ] 进度展示界面
- [ ] 停止/暂停控制

### 优先级 2: 功能完善
- [ ] AI 模型选择 UI
- [ ] 模型切换功能
- [ ] 应用图标设计
- [ ] 启动画面设计

### 优先级 3: i18n 迁移
需要迁移的 crates:
- [ ] title_bar
- [ ] workspace
- [ ] settings_ui
- [ ] agent_ui

---

## 依赖关系图

```
zed (主程序)
├── autonomous_agent (自主编程)
│   └── language_model
├── cc_switch (控制中心)
│   ├── gpui
│   ├── i18n
│   └── settings
├── multi_model_dispatch (多模型调度)
│   ├── gpui
│   └── language_model
├── agent_ui (Agent 界面)
│   └── agent
└── i18n (国际化)
```

---

## 任务派发指南

### Claude Agent 适合的任务
- i18n 国际化迁移
- UI 字符串翻译
- 文档编写
- 代码审查

### Gemini Agent 适合的任务
- 架构设计
- 新模块开发
- 复杂逻辑实现
- 集成测试

### Codex Agent 适合的任务
- Bug 修复
- 测试编写
- 代码清理
- 编译错误修复

---

## 编译命令

```bash
# 检查单个 crate
cargo check -p cc_switch
cargo check -p multi_model_dispatch
cargo check -p autonomous_agent

# 运行测试
cargo test -p cc_switch
cargo test -p i18n

# 编译完整应用
cargo build -p zed --release

# 运行
.\target\release\zed.exe
```

---

## 配置文件位置

| 配置 | 路径 |
|------|------|
| Claude | `~/.claude/` |
| Codex | `~/.codex/config.toml` |
| Gemini | `~/.gemini/` |
| MCP 服务器 | 各 CLI 配置目录 |

---

## Git 仓库

- **远程**: https://github.com/yanghao1143/AI-code-generation
- **分支**: main

---

*最后更新: 2026-02-01*
*维护者: OpenClaw 指挥官*
