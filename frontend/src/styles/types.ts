// Design token types and scheme keys
export type Tokens = Record<string, string>;

// Preset scheme keys (aligned with current /style page)
export type SchemeKey = "A" | "B" | "C" | "D";

// Theme name used for html[data-theme]
export type ThemeName = "light" | "dark";